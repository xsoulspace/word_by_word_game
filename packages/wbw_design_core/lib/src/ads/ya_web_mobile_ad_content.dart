import 'package:flutter/material.dart';

class YandexAdMobileContent extends StatelessWidget {
  const YandexAdMobileContent({super.key});

  @override
  Widget build(final BuildContext context) {
    return Container();
  }
}
