import 'package:flutter/widgets.dart';

class NextTechnologyBadge extends StatelessWidget {
  const NextTechnologyBadge({super.key});

  @override
  Widget build(final BuildContext context) => const Placeholder();
}
