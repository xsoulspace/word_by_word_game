part of pack_game;

class GameBookWriterScreen extends HookWidget {
  const GameBookWriterScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    final state = useGameBookWriterScreenState();
    return Container();
  }
}
