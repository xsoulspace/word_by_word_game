part of pack_game;

class GlobalMenuScreen extends HookWidget {
  const GlobalMenuScreen({final Key? key}) : super(key: key);

  @override
  Widget build(final BuildContext context) {
    final state = useGlobalMenuScreenState();
    return Container();
  }
}
